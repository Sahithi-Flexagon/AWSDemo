{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "The template used to create a stack from FDTLT88",
  "Resources": {
    "DemoResourceGroup": {
      "Type": "AWS::ResourceGroups::Group",
      "Properties": {
        "Name": "DemoResourceGroup",
        "Description": "test resource group"
      }
    }
  }
}
